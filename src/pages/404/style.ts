import styled from 'styled-components'
import { S_HOME } from '../home/style'

export const S_ERROR = styled(S_HOME)`
    color: #333;
    font-size: 1.2rem;
    display:flex;
    flex-direction: column;
    justify-content: center;
    align-items:center;
    gap: 1rem;

    a{
        color: #047;
    }

`
